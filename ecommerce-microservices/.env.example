# Copy to .env and fill in real values before running docker-compose or
# deploying. Never commit a real .env file.

JWT_SIGNING_KEY=replace-with-a-long-random-secret
INTERNAL_SERVICE_TOKEN=replace-with-a-long-random-secret

POSTGRES_USER=ecommerce
POSTGRES_PASSWORD=replace-with-a-strong-password
