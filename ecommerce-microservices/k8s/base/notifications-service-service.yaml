apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: ecommerce
  labels:
    app: notifications-service
spec:
  selector:
    app: notifications-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
